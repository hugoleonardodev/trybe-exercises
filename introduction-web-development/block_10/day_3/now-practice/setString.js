const stringToUpperCase = (string) => string.toUpperCase();

const stringFirstLetter = (string) => string[0];

const concatStrings = (stringA, stringB) => stringA + stringB;

module.exports = {stringToUpperCase, stringFirstLetter, concatStrings}